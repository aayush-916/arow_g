# Disable Netlify "Pretty URLs" so /index.html is NOT redirected to / .
# A redirect on the PWA start_url makes the installed app launch unreliably
# (opens only after several taps).
[build.processing]
  skip_processing = false

[build.processing.html]
  pretty_urls = false
